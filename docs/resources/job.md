---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "jenkins_job Resource - jenkins"
subcategory: ""
description: |-
  Manages a job (or folder-scoped job) within Jenkins from a raw config.xml template.
---

# jenkins_job (Resource)

Manages a job (or folder-scoped job) within Jenkins from a raw config.xml template.

## Example Usage

```terraform
resource "jenkins_folder" "team" {
  name = "platform-team"
}

resource "jenkins_job" "deploy" {
  name   = "deploy-backend"
  folder = jenkins_folder.team.id

  template = templatefile("${path.module}/pipeline.xml", {
    repo_url       = "https://github.com/example/backend.git"
    default_branch = "main"
  })
}
```

And in `pipeline.xml`:

```xml
<flow-definition plugin="workflow-job@2.25">
  <actions/>
  <description>Deploys the backend service</description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsScmFlowDefinition" plugin="workflow-cps@2.59">
    <scm class="hudson.plugins.git.GitSCM" plugin="git@3.9.1">
      <configVersion>2</configVersion>
      <userRemoteConfigs>
        <hudson.plugins.git.UserRemoteConfig>
          <url>${repo_url}</url>
          <credentialsId>github</credentialsId>
        </hudson.plugins.git.UserRemoteConfig>
      </userRemoteConfigs>
      <branches>
        <hudson.plugins.git.BranchSpec>
          <name>*/${default_branch}</name>
        </hudson.plugins.git.BranchSpec>
      </branches>
      <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
      <submoduleCfg class="list"/>
      <extensions/>
    </scm>
    <scriptPath>Jenkinsfile</scriptPath>
    <lightweight>true</lightweight>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The unique name of the JenkinsCI job.
- `template` (String) The Jenkins-compatible XML template describing the job. An existing job's XML can be retrieved by appending `/config.xml` to its URL. Rendered as a Go template with the other resource arguments available as variables; do not include the XML prolog. Well-formedness is validated at plan time, and semantically-equivalent XML (differing only in attribute order, empty-element syntax, whitespace, plugin versions, or the XML declaration) does not produce a diff.

### Optional

- `disabled` (Boolean) Whether the job is disabled. When set, the provider enforces this state through the Jenkins enable/disable API and it takes precedence over any `<disabled>` element in the template; an out-of-band toggle is detected as drift. When omitted, the provider does not manage the job's enabled state and the template's own `<disabled>` value, if any, applies.
- `folder` (String) The folder namespace to create this job in. Nested folders are separated with `/`, e.g. `parent/child`. Cannot be changed after creation, and every parent folder must already exist.

### Read-Only

- `id` (String) The full canonical job path, e.g. `/job/job-name`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Jobs are imported by their canonical path.
terraform import jenkins_job.deploy /job/platform-team/job/deploy-backend
```

~> The imported XML may not match your `template` exactly. Run `terraform plan` after importing, and apply any resulting template diff so state and configuration agree.
